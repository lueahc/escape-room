export interface EnvironmentVariable {
    DB_TYPE: string;
    DB_HOST: string;
    DB_PORT: number;
    DB_DATABASE: string;
    DB_USERNAME: string;
    DB_PASSWORD: string;
    DB_SYNCHRONIZE: boolean;
}